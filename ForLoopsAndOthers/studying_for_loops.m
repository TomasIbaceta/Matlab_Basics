clc
clear

%------------------- nested for loops example -----------------
%     for i = 1:3
%         for j = 1:3
%            product = i*j;
%            fprintf("%.2f * %.2f = %.2f\n", i,j,product)
%         end
%     end

%------------ complex operations example ---------------
%c = 7 + 1j*3;
%R = real(c);
%X = imag(c);
%mod = abs(c);
%ang = angle(c);
%ans2 = sqrt(7^2 + 3^2);

%------------ solve cuadratics and use if example -------
%para Ax^2 + Bx + C

% a = input ('A: ');
% b = input ('B: ');
% c = input ('C: ');
% discriminant = b^2-4*a*c; 
% if discriminant == 0
%     fprintf('the root is %f', -b/(2*a) )
% else
%     fprintf('the roots are %f and %f',(-b+sqrt(b^2-4*a*c))/(2*a),(-b-sqrt(b^2-4*a*c))/(2*a))
% end

